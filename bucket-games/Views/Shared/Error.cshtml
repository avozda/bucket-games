@model ErrorViewModel
@{
    ViewData["Title"] = "Chyba";
}

<h1 class="text-danger">Upsie.</h1>
<h2 class="text-danger">Chybička se vloudila.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}
